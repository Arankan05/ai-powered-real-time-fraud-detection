# Scripts

Developer convenience scripts for common tasks.

## Planned Scripts

| Script | Purpose |
|---|---|
| `start-dev.sh` / `start-dev.ps1` | Start all local services (Docker + backend + frontend) |
| `seed-db.py` | Load synthetic data into the development database |
| `train-model.py` | Run the ML training pipeline |
| `lint.sh` / `lint.ps1` | Run linters across all modules |
| `test.sh` / `test.ps1` | Run all test suites |

Scripts will be created as needed during the implementation phase.

## Status

Not yet implemented. Foundation placeholder only.
