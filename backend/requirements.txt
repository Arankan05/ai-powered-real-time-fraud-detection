# ==============================================================================
# Backend - Python Dependencies
# ==============================================================================

# --- Web Framework ---
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.30.0,<1.0.0

# --- Validation & Configuration ---
pydantic[email]>=2.0.0,<3.0.0
pydantic-settings>=2.0.0,<3.0.0
email-validator>=2.0.0,<3.0.0

# --- Authentication & Security ---
PyJWT>=2.8.0,<3.0.0
bcrypt>=4.0.0,<5.0.0

# --- Database ---
sqlalchemy>=2.0.0,<3.0.0
psycopg[binary,pool]>=3.1,<4.0.0
alembic>=1.13.0,<2.0.0

# --- HTTP Client (ML service integration) ---
httpx>=0.27.0,<1.0.0

# --- ML Integration (feature engineering used by backend/ML tests) ---
numpy>=1.24.0,<3.0.0
pandas>=2.0.0,<3.0.0

# --- ML Model Training (ml/ pipeline: XGBoost classifier) ---
xgboost>=2.0.0,<3.0.0

# --- ML Explainability (ml/ pipeline: SHAP feature attributions) ---
shap>=0.44.0,<1.0.0

# --- Testing ---
pytest>=8.0.0,<9.0.0
pytest-asyncio>=0.23.0,<1.0.0
pytest-cov>=5.0.0,<6.0.0

# --- Code Quality ---
black>=24.0.0,<25.0.0
ruff>=0.6.0,<1.0.0
